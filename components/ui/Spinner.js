import React from 'react';
import Layout from '../Layout';

const Spinner = () => {
    return (
        <>
            <Layout>
                <p>Cargando...</p>
            </Layout>
        </>
    );
}

export default Spinner;